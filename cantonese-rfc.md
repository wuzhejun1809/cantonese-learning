# 粤语学习系统 V2 技术规范（RFC）

> 版本：2.3 | 生成日期：2026-07-18 | 更新日期：2026-07-23 | 状态：生效中

---

## 一、Must Not Change（禁止修改）

以下内容已经过用户确认，后续自动化执行时**不得更改**：

### 1.1 部署架构
- **单文件 HTML 部署**：所有 CSS、JS 内嵌在一个 `.html` 文件中
- **音频外部引用**：音频文件存放于 `audio/dayN/` 子目录，HTML 用相对路径 `<audio src="audio/dayN/wM.mp4">` 引用（⚠️ 必须用 `-f mp4f` 生成 ISO MP4 v2 容器，不能用 `-f m4af`。2026-07-23 从 `.m4a` M4A 容器改为 `.mp4` MP4 v2 容器以修复手机端兼容性）
- **文件名固定**：第一部为 `cantonese-learning.html`，后续周期为 `cantonese-cycle2.html`、`cantonese-cycle3.html`，以此类推
- **索引页**：`cantonese-index.html` 作为入口，链接所有周期文档

### 1.2 学习规则
- 每天 5 个粤语实用单词（生活 + 职场优先）
- 每 3 天一次 Mini Review：学 5 新词 + 回顾前 3 天全部单词
- 每 9 天一次大回顾：不出新词，45 词全面复盘 + 场景对话测试
- 大回顾日**附音频**（引用已有 audio ID，不重新生成），小回顾日照常

### 1.3 音频方案
- 使用 `<audio>` 元素 + 外部文件引用 `src`，每个音频一个唯一 ID（如 `audio-d1w1`）
- 回顾章节引用**同一 ID**，不重复创建 audio 元素
- 音频文件命名规则：`audio/dayN/{type}M.mp4`，其中 type=w（单词）/s（例句）/t（测试），M=1-5
- 音频生成：macOS `say -v Sinji`（必须用繁体字）→ AIFF → `afconvert -f mp4f -d aac` → `.mp4`（⚠️ 必须用 `-f mp4f` 不用 `-f m4af`——`mp4f` 生成 ISO MP4 v2 标准容器，手机端兼容；`m4af` 生成 Apple M4A 容器，移动浏览器可能拒绝播放）
- 音频存储：`audio/dayN/` 目录，部署时随 HTML 一起上传到 CloudStudio
- **不得使用 base64 内嵌**（会导致 HTML 文件膨胀，自动化执行时 token 溢出）

### 1.4 UI 结构
- **桌面端**：左侧固定侧边栏导航（240px），可点击跳转，滚动时自动高亮当前 Day
- **手机端**：底部固定浮动按钮（☰），点击弹出菜单
- **回顾导航**：回顾章节有独立 anchor（如 `#day3-review`），在导航中作为紫色独立项
- **侧边栏统计**：显示已学单词数、下次小回顾、下次大回顾日期
- **索引页链接**：侧边栏和浮动菜单顶部都有"🏠 索引页"链接

### 1.5 单词内容格式
每个单词必须包含：
1. **词义**：普通话释义 + 使用场景
2. **发音**：粤拼（jyutping）+ 发音提示（近似普通话读音 + 声调标注）
3. **音频**：单词音频 + 例句音频（各一条）
4. **例句**：粤语例句 + 普通话翻译 + 逐字粤拼和释义（粤语特有字红色高亮）
5. **词源解析**：字源、造字逻辑或用法演变
6. **记忆窍门**：一条便于记忆的小提示

### 1.6 历史内容保护
- **禁止修改已生成的 Day 内容**：Day 1–Day N 的 section 内容一旦生成，后续执行只能新增 Day，不得重构、重写或"优化"已有内容
- **允许的操作**：更新侧边栏统计数字、更新索引页统计、新增 Day section、新增回顾 section

---

## 二、Required Changes（必须实现）

### 2.1 新增 Day 时的必做清单
每次自动化执行生成新 Day 时，必须完成以下全部步骤：

1. **生成 5 个新单词的完整内容**（词义/发音/音频/例句/词源/记忆窍门）
2. **生成音频**：5 条单词音频 + 5 条例句音频，用 `say -v Sinji` 繁体字生成
3. **将音频嵌入 HTML**：新增 `<audio>` 元素（唯一 ID），base64 格式
4. **新增 `<section>`**：追加到 `</main>` 之前，有唯一 anchor ID（如 `#day4`）
5. **更新侧边栏**：新增 Day 导航链接
6. **更新浮动菜单**：同步新增 Day 导航链接
7. **更新侧边栏统计**：已学单词数 +5、学习天数 +1、下次回顾日期
8. **更新索引页**：已学单词数、学习天数、新增主题标签
9. **更新自动化 memory.md**：记录当天单词列表
10. **部署到 CloudStudio**：使用 `workbuddy_cloudstudio_deploy` 工具

### 2.2 Mini Review 日（每 3 天）额外必做
1. 新增 5 个新词（同上）
2. 新增回顾 section（有独立 anchor，如 `#day6-review`）
3. 回顾表格：列出前 3 天全部单词，每行附音频按钮（引用已有 audio ID）
4. 5 道场景对话测试题（听音频填空，可点击揭示答案）
5. 测试题音频：生成 5 条新音频（`test1–5`），嵌入 HTML

### 2.3 大回顾日（每 9 天）额外必做
1. **不出新词**
2. 回顾 section 涵盖当周期全部 40 词
3. **附音频**（回顾表每行附 🔊 按钮引用已有 audio ID，场景对话附朗读按钮引用已有例句 audio ID）
4. 场景对话测试：3–5 个综合场景
5. 创建新周期文件（如 `cantonese-cycle2.html`），从头开始
6. 更新索引页：新增周期卡片

### 2.4 日期计算规则
- Day 1 = 2026-07-16
- Day N = 2026-07-16 + (N-1) 天
- Mini Review：Day 3, 6, 9...（每 3 天）
- 大回顾：Day 9, 18, 27...（每 9 天）
- 侧边栏和索引页的日期必须据此计算，不得手写

---

## 三、Nice to Have（未来预留）

以下功能**当前不实现**，但代码结构不得阻碍未来扩展：

- **搜索**：在页面内搜索单词
- **收藏**：标记重点单词
- **Cross Link**：不同 Day 之间的关联词链接
- **进度统计**：学习进度条、掌握度标记
- **导出**：导出为 PDF 或 Anki 卡片

预留方式：HTML 结构保持语义化（用 `<section>`、`<div class="word-card">` 等），ID 命名保持一致规范（`dayN`、`audio-dNwN`），方便未来 JS 选择器匹配。

---

## 四、Acceptance Criteria（验收标准）

每次自动化执行完成后，必须全部通过以下检查：

### 4.1 HTML 结构完整性
- [ ] `<body>` 1 个开 1 个闭
- [ ] `<main>` 1 个开 1 个闭
- [ ] `<nav>` 1 个开 1 个闭
- [ ] `</html>` 1 个闭
- [ ] 无重复 `<section id="dayN">`

### 4.2 音频完整性
- [ ] 每个新 Day 的 5 单词 + 5 例句 = 10 条音频已生成
- [ ] 每条音频有唯一 `<audio id="audio-dNwN">` 元素
- [ ] base64 数据非空（长度 > 100 字符）
- [ ] 所有按钮 `onclick` 引用的 ID 都存在对应的 `<audio>` 元素
- [ ] 回顾章节引用已有 audio ID，不重复嵌入 base64

### 4.3 导航完整性
- [ ] 侧边栏包含所有 Day 链接
- [ ] 浮动菜单包含所有 Day 链接
- [ ] 回顾章节有独立 anchor 并在导航中列出
- [ ] 所有 `href="#xxx"` 链接指向存在的 `id="xxx"` 元素
- [ ] 索引页链接指向正确的周期文件

### 4.4 内容完整性
- [ ] 无重复单词（与 memory.md 已教列表比对）
- [ ] 无重复例句
- [ ] 侧边栏统计数字正确（已学单词数 = Day × 5）
- [ ] 索引页统计与侧边栏一致
- [ ] 日期计算正确

### 4.5 部署验证
- [ ] CloudStudio 部署成功（`verified: true`）
- [ ] 学习页 URL 可访问
- [ ] 索引页 URL 可访问
- [ ] 音频按钮可点击播放

### 4.6 历史内容保护
- [ ] 已有 Day 的 section 内容未被修改
- [ ] 已有 audio ID 未被重命名
- [ ] 已有 anchor 未被删除或重命名

---

## 附录：文件结构

```
/Users/user/WorkBuddy/automation-2026-07-16-01-42-07/
├── cantonese-learning.html      # 第一部（Day 1-9）
├── cantonese-index.html          # 索引页
├── cantonese-rfc.md              # 本文档
├── audio/
│   ├── day1/                     # word1-5.mp4, sentence1-5.mp4
│   ├── day2/
│   └── day3/                     # + test1-5.mp4
└── .workbuddy/
    ├── automations/
    │   └── automation-1784137327443/
    │       └── memory.md         # 执行记录 + 单词列表
    └── memory/
        ├── MEMORY.md             # 项目记忆
        └── 2026-07-18.md         # 每日工作日志
```
